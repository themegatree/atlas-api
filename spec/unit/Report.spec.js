describe('Test Report Class', () => {
  it('can get all of the report', ()=> {
    //const report = require('../../src/reports/Report');
    //const CohortId = 1;
    //const reportObj = await Report(CohortId);

    
  });

  it('can get only gender stats', ()=> {

  });
  
});
